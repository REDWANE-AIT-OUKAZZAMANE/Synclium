name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Lint, typecheck, test, build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 10.29.2

      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install ${{ github.actor == 'dependabot[bot]' && '--no-frozen-lockfile' || '--frozen-lockfile' }}

      - name: Build packages
        run: pnpm -r build
        env:
          NEXT_TELEMETRY_DISABLED: 1

      - name: Typecheck
        run: pnpm -r typecheck

      - name: Test
        run: pnpm -r test

      - name: Smoke-test CLI
        run: |
          node packages/cli/dist/bin.js detect examples/zatca/standard-invoice.xml
          node packages/cli/dist/bin.js convert examples/ubl/invoice-basic.xml --to canonical > /tmp/canonical.json
          node packages/cli/dist/bin.js validate examples/facturx/en16931-full.xml
